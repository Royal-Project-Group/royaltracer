Installation of required dependencies:
- Install vcpkg in clion / your prefered IDE
- Install all libs listed in the vcpkg.json file (this might work automatically)
- Download and extract the OIDN 2.1.0 from the respective web page
- In CMakeLists.txt, change the OIDN_ROOT variable to the filepath to OIDN on your system